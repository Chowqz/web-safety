<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登录</title>
    <link rel="stylesheet" type="text/css" href="/style/reset.css">
    <style type="text/css">
    .form-box {
        width: 400px;
        border: 1px solid #ddd;
        padding: 20px;
        position: absolute;
        top: 20%;
        left: 50%;
        transform: translateX(-50%);
    }

    .form-box-title {
        text-align: center;
        font-size: 16px;
        margin-bottom: 10px;
    }

    .form-item {
        margin-bottom: 20px;
    }

    .form-input {
        width: 100%;
        height: 36px;
        text-indent: 10px;
        border: 1px solid #ddd;
        border-radius: 4px;
    }

    .submit-btn {
        width: 100%;
        height: 38px;
        color: #fff;
        background-color: #286090;
    }
    </style>
</head>

<body>
    <div class="form-box">
        <h4 class="form-box-title">登录</h4>
        <div class="form-item">
            <input class="form-input" id="userName" type="text" name="userName" placeholder="用户名">
        </div>
        <div class="form-item">
            <input class="form-input" id="password" type="password" name="password" placeholder="密码">
        </div>
        <div class="form-item">
            <button class="submit-btn">登录</button>
        </div>
    </div>
    <script type="text/javascript" src="/js/libs/jquery-3.6.0.min.js"></script>
    <script type="text/javascript">
        $(document).ready(function() {
            $('.submit-btn').click(function() {
                $.ajax({
                    url: '/api/user/login',
                    method: 'post',
                    data: {
                        userName: $('#userName').val(),
                        password: $('#password').val()
                    },
                    success: function(res) {
                        console.log(res)
                        if(res.code === '0') {
                            location.href = '/view/index';
                        }
                        else {
                            alert(res.msg);
                        }
                    },
                    error: function(err) {
                        alert(err);
                    }
                })
            })
        })
    </script>
</body>

</html>